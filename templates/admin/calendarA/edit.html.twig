{% extends 'base.html.twig' %}

{% block title %}Modifier la réservation
{% endblock %}

{% block body %}
	<h1 class="text-center">Modifier la réservation</h1>

	<section class="container">
		<div class="row justify-content-center">
			<div class="col-12 text-center mt-4 mb-3">
				{{ include('admin/calendarA/_form.html.twig', {'button_label': 'Modifier'}) }}

				
			</div>
            <div class="mt-3 text-center">
					{{ include('admin/calendarA/_delete_form.html.twig') }}
                   
				</div>
		</div>
         <div class="mt-3 text-center">
					<a class="btn btn mr-2" href="{{ path('admin_calendar_index') }}">Retour à la liste</a>
                </div>
	</section>
{% endblock %}

{% block javascripts %}
	{#<script src="{{ asset('assets/js/editCalendar.js') }}" defer></script>#}
	<script>
		let closedDays = {{ closedDaysArray | json_encode | raw }};
	</script>
	<script src="{{ asset('assets/js/calendarCloseDay.js') }}" defer></script>
	<script src="{{ asset('assets/js/reservationVerif.js') }}" defer></script>
{% endblock %}
